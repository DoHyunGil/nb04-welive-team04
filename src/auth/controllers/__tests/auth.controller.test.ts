// 예시로 쓰라고 남겨둔 더미 파일이에요
// 실제 auth controller 테스트는 src/routers/__tests__/auth.route.test.ts에 있습니다.

describe('Auth Controller', () => {
  it('placeholder test', () => {
    // 실제 테스트는 auth.route.test.ts에서 수행합니다.
    expect(true).toBe(true);
  });
});
