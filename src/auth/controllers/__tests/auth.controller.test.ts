// import { prisma } from '../../../lib/prisma.js';

// test('GET /detail', async () => {
//   const data = await prisma.test.create({ data: { name: 'TEST' } });
//   expect(data.name).toEqual('TEST');
// });

//예시로 쓰라고 남겨둔 더미 파일이에요
